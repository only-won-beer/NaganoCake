<div class="container">
  <h3 class="mini-title">注文履歴一覧</h3>
</div>
<div class="container">
  <div class="row">
    <table class="table table-bordered table-background">
      <thead class="thead-dark">
        <tr>
          <th>注文日</th>
          <th>配送先</th>
          <th>注文商品</th>
          <th>支払い金額</th>
          <th>ステータス</th>
          <th>注文詳細</th>
        </tr>
      </thead>
      <tbody>
        <% @orders.each do |order| %>
          <tr>
            <td class="text-light">
              <%= order.created_at.strftime('%Y/%m/%d') %><br>
              <%= order.created_at.strftime('%H:%M') %>
            </td>
            <td class="text-light">
              <%= order.postcode %><br>
              <%= order.address %><br>
              <%= order.address_name %>
            </td>
            <td class="text-light">
              <% order.items.each do |item| %>
                <%= item.name %><br>
              <% end %>
            </td>
            <td class="text-light">
              <%= order.total_pay %>
            </td>
            <td class="text-light">
              <%= order.status_i18n %>
            </td>
            <td><%= link_to "表示する", order_path(order), class:"text-white" %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>      